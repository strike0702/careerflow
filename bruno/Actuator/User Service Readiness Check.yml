info:
  name: User Service Readiness Check
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8081/actuator/health/readiness
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
