info:
  name: Gateway Readiness Check
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:9000/actuator/health/readiness
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
