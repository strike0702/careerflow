info:
  name: Gateway Health Check
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:9000/actuator/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
