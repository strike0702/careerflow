info:
  name: Gateway Prometheus Metrics
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:9000/actuator/prometheus
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
