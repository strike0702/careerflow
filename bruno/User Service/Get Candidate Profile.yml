info:
  name: Get Candidate Profile
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/api/v1/users/me/profile
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
