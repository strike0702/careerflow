info:
  name: Update Candidate Profile
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8081/api/v1/users/me/profile
  auth: inherit
  body:
    type: json
    data: |-
      {
        "targetRoles": "Staff Backend Engineer",
        "targetSalaryMin": 150000.0,
        "targetSalaryMax": 190000.0,
        "skills": ["Java 21", "Spring Boot 3", "Kubernetes"]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
