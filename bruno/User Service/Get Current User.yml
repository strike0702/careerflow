info:
  name: Get Current User
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/api/v1/users/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
