info:
  name: Get Dashboard
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8083/api/v1/applications/dashboard
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
