info:
  name: Get Application Details
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8083/api/v1/applications/{{applicationId}}
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
