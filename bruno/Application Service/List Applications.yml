info:
  name: List Applications
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8083/api/v1/applications?status=INTERVIEWING&company=Google&page=0&size=20
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
